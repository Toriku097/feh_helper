import 'package:flutter/material.dart';

class VsManyBattle extends StatelessWidget {
  const VsManyBattle({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}